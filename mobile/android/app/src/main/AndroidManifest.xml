<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- ══ Permisos del spike ══════════════════════════════════════════════
         CAMERA y RECORD_AUDIO: necesarios para publicar en LiveKit.
         MODIFY_AUDIO_SETTINGS: WebRTC lo pide para el enrutado de audio.
         ACCESS_NETWORK_STATE: connectivity_plus detecta WiFi ⇄ 4G, que es uno
                               de los escenarios que este spike tiene que medir.
         WAKE_LOCK: la pantalla no puede apagarse a mitad de una medición.
         BLUETOOTH_CONNECT: auriculares BT durante la transmisión (Android 12+).
    ═══════════════════════════════════════════════════════════════════════ -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:usesPermissionFlags="neverForLocation" tools:targetApi="s"/>

    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- La política de red va en un recurso, no acá.
         Antes esta etiqueta tenía usesCleartextTraffic="true" para toda la app
         —puesto en el spike, para hablar con la notebook por http://— y con el
         botón "Configurar servidor" al lado era un camino completo de
         degradación: apuntar la app a un http:// cualquiera y ver pasar
         sesiones, direcciones y pedidos en texto plano.
         Ahora depende del tipo de compilación: release sólo habla HTTPS,
         debug y profile permiten texto plano para la prueba de campo. Ver
         res/xml/network_security_config.xml. -->
    <application
        android:label="VendoX"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
              ══════════════════════════════════════════════════════════════════
              ACÁ NO HAY UN intent-filter PARA vendox.com.ar, Y ES A PROPÓSITO
              ══════════════════════════════════════════════════════════════════

              El servidor ya sirve `/.well-known/assetlinks.json` con las huellas
              cargadas, así que la mitad de Android está lista. Falta la otra
              mitad: **la app no sabe qué hacer con un enlace entrante.**

              No hay `app_links` ni `uni_links` en `pubspec.yaml`, ni nada que
              lea el enlace con el que se abrió el proceso. Con el filtro puesto,
              tocar `vendox.com.ar/p/prd_123` abriría la app en el feed en vez de
              en ese producto — y hoy ese mismo enlace abre una landing que sí lo
              muestra, con su foto y su precio.

              O sea que activarlo ahora **empeora** el resultado. Un enlace
              compartido por WhatsApp dejaría de llevar a donde lleva hoy.

              ─── Qué falta, exactamente ───

                1. Un paquete que entregue el enlace inicial y los siguientes.
                2. Un enrutador que traduzca /p/, /v/, /t/ y /u/ a las pantallas.
                3. Y que una ruta desconocida —/privacidad, por ejemplo— vuelva
                   al navegador en vez de quedarse en una pantalla en blanco.

              Recién con eso, este bloque va acá y toma el dominio entero (sin
              `pathPrefix`, para que agregar una ruta en el backend no obligue a
              publicar una versión nueva de la app):

                  <intent-filter android:autoVerify="true">
                      <action android:name="android.intent.action.VIEW"/>
                      <category android:name="android.intent.category.DEFAULT"/>
                      <category android:name="android.intent.category.BROWSABLE"/>
                      <data android:scheme="https" android:host="vendox.com.ar"/>
                  </intent-filter>
            -->
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
